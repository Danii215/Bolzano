import { Meta, Story, Canvas } from "@storybook/addon-docs";
import * as Footer from "./Footer.stories";

<Meta title="Footer" of={Footer} />

# Documentação da molécula Footer

---

O `footer` é a parte mais "baixa" do website, literalmente os pés da página.
A ideia é ser exibido em todas as páginas e faz parte do layout, por último.

-   **Componente de Servidor**

## Props

Não aplicável.

## Stories

### Default

<Canvas of={Footer.Default} />

## Exemplo de uso

```tsx
import { Footer } from "./Footer";

function App() {
    return <Footer />;
}
```
